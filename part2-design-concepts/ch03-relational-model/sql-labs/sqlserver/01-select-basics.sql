-- 01 - Basic SELECT Queries (TinyCollege)

-- Select all students
SELECT * FROM STUDENT;

-- Select specific columns: student number, last name, first name
SELECT STU_NUM, STU_LNAME, STU_FNAME
FROM STUDENT;
